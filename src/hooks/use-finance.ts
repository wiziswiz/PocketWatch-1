/**
 * Backward-compatibility re-export shim.
 * All hooks have been split into src/hooks/finance/*.
 * Import from there directly, or continue using this path — both work.
 */

export * from "./finance/index"
